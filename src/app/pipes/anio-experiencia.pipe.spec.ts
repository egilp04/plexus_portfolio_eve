import { AnioExperienciaPipe } from './anio-experiencia.pipe';

describe('AnioExperienciaPipe', () => {
  it('create an instance', () => {
    const pipe = new AnioExperienciaPipe();
    expect(pipe).toBeTruthy();
  });
});
