import { GiroDirective } from './giro.directive';

describe('GiroDirective', () => {
  it('should create an instance', () => {
    const directive = new GiroDirective();
    expect(directive).toBeTruthy();
  });
});
